{
  "dependencies": {
    "moment": "^2.20.1",
    "normalize.css": "^7.0.0",
    "requirejs": "^2.3.2"
  },
  "devDependencies": {
    "http-server": "^0.10.0"
  },
  "scripts": {
    "build": "r.js -o baseUrl=js paths.moment=../node_modules/moment/moment name=main out=js/bundle.js",
    "prestart": "npm run-script build",
    "start": "http-server"
  }
}
